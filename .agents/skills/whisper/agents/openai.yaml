interface:
  display_name: "Whisper"
  short_description: "Repo-specific patterns and workflows for whisper"
  default_prompt: "Use the whisper repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true